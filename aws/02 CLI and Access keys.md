- each user manages his own access keys
- we can create access keys from user > security credentials
- after creating access key we can configure aws cli with
> aws configure
- this will ask for the access key ID, the access key and the region
- we can check it work with
> aws iam list-users